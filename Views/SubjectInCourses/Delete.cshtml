@model Vidhyalaya.DB.SubjectInCourse

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>Subject In Course</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Course.CourseName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Subject.SubjectName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Subject.SubjectName)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a type="button" class="btn btn-primary btn-sm"
               href="@Url.Action("GetMapCourseSubject", "MapCourseSubject")">
                <span class="glyphicon glyphicon-list"></span>
                <span>Back to list</span>
            </a>
        </div>
    }
</div>